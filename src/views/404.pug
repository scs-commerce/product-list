<!DOCTYPE html>
html(lang="en")
  head
    meta(charset="UTF-8")
    title Document not found
  body
    h1 Document not found

    p It seems that the document you are looking for, does not exist.

    p
      | You may want to start at the
      = " "
      a(href="/products") products
      = " "
      | page
